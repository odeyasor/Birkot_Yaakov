



פתיחת וסגירת המערכת באופן אוטומטי מיום חמישי 10:10 עד יום שלישי 08:00
כאשר נסגרת המערכת כל הסלים נמחקים. כאשר נפתחת ההזמנות מועברות לטבלת הסטוריה- שבה בנוסף לשדות הרגילים גם שדה תאריך של אותו יום ראשון.

לעשות וולידצית מייל יפה עם שליחת מייל לאימות




export const findDirecByDirecName = async (direcName, direcPwd, privatePwd, email) => {
    const direc = await Direc.findOne({ direcName });
    if (direc)
        return direc;
    return res.status(501).send('director not found')
};
